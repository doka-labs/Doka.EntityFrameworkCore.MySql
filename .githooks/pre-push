#!/usr/bin/env bash

# Mirrors the complete CI quality gate before commits leave the workstation.

set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"

echo "Running full repository quality gates before push..."
exec "${repo_root}/eng/quality-gates.sh"
