# Relationships

Demonstrates one-to-many, many-to-many, and self-referencing relationships,
including `Include`-based eager loading.

```bash
dotnet run --project examples/Relationships/Relationships.csproj
```

The example persists and reads every relationship shape before deleting its
isolated schema.
